/**
 * Shared Components
 *
 * This directory contains reusable components shared across multiple features.
 * These are higher-level components (molecules/organisms) composed from UI primitives.
 *
 * Examples:
 * - SearchBox - Search input with button
 * - DataTable - Reusable table with sorting/filtering
 * - Modal - Generic modal wrapper
 * - Toast - Notification system
 */

// Shared components barrel export
// Example: export { SearchBox } from "./SearchBox";
