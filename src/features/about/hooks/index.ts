/**
 * About Feature Hooks
 *
 * This directory contains custom React hooks specific to the about feature.
 * All hooks here are scoped to the about module only.
 */

// About hooks barrel export
// Example: export { useTeamData } from "./useTeamData";
